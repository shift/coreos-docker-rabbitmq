machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t rabbitmq .
test:
  override:
    - docker run; sleep 20
    - curl -i --user guest:guest http://localhost:15673/api/vhosts | grep "200 OK"
